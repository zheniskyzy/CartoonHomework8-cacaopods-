# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CartoonHomework8(cacaopods)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for CartoonHomework8(cacaopods)
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'SVProgressHUD'
  pod 'SDWebImage'
end
